
<div class="container text-center container-size">

  <h1 id="index" class="index-h1">Our Selection of Caves</h1>

    <div class="row">
        <% @caves.each do |cave| %>
          <div id="col" class="col-sm-3" >
            <div class="cave-card card">
              <%= cl_image_tag(cave.photo, width: 200, height: 175, crop: :fill, class: 'card-img-border') %>
              <div class="cave-card-body card-body">
                <h3 class="card-title"> <%= cave.name %></h3>
                <%= link_to 'Visit', cave_path(cave), class: "btn btn-info btn-sm" %>
              </div>
            </div>
          </div>
        <% end %>s
    </div>

</div>
