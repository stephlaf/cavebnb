<div class="container">
  <div class="row">
      <div class="col-sm-12 col-md-6">
  <h2 class="text-center">Your Upcoming Harvest</h2>
  <div class="cards">
<!--renter -->
    <% @sent_bookings.each do |booking| %>
      <div class="card col-sm-8 col-md-4">
        <div class="card-body" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.1)), url(<%= booking.cave.photo %>);">
          <span class="card-category"><h3><%= booking.cave.name %></h3></span>
          <div class="card-description">
            <h2>Begins: <%= booking.checkout.strftime("%A, %B %d, %Y") %></h2>
          </div>
          <img class="avatar" src="<%= booking.cave.user.photo %>">
        </div>
        <div class="card-footer">
          <p>Posted on 22/11/19</p>
          <div class="links">
            <a href="#"><i class="fa fa-camera-retro"></i></a></li>
            <a href="#"><i class="fa fa-coffee"></i></a></li>
            <a href="#"><i class="fa fa-poo"></i></a></li>
          </div>
        </div>
      </div>
    <% end %>

  </div>
</div>

      <div class="col-sm-12 col-md-6">
  <h2 class="text-center">Your Hoards of Renting Hordes</h2>
  <div class="cards">
<!--renter -->
  <% @sent_bookings.each do |booking| %>
    <div class="card col-sm-8 col-md-4">
      <div class="card-body" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.1)), url(<%= booking.cave.photo %>);">
        <span class="card-category"><h3><%= booking.cave.name %></h3></span>
        <div class="card-description">
          <h2>Begins: <%= booking.checkout.strftime("%A, %B %d, %Y") %></h2>
        </div>
        <img class="avatar" src="<%= booking.cave.user.photo %>">
      </div>
      <div class="card-footer">
        <p>Posted on 22/11/19</p>
        <div class="links">
          <a href="#"><i class="fa fa-camera-retro"></i></a></li>
          <a href="#"><i class="fa fa-coffee"></i></a></li>
          <a href="#"><i class="fa fa-poo"></i></a></li>
        </div>
      </div>
    </div>


  <% end %>

  </div>
</div>

  </div>
</div>

<!-- owner -->

<h1>Your Bookings (as Owner)</h1>
<% @received_bookings.each do |booking| %>
  <div>
    <%= cl_image_tag booking.cave.photo, height: 150, width: 150, crop: :thumb %>
    <span><%= link_to 'Get Details', booking_path(booking), class: 'btn btn-info' %></span>
  <!-- <span><%#link_to 'Edit', edit_path(booking), class: 'btn btn-treehouse' %></span> -->
    <span><%= link_to 'Destroy!', booking_path(booking), class: 'btn btn-danger',  method: :delete,
                           data: { confirm: 'Are you sure?' } %></span>
  </div>

      <div>
      <h3><%= booking.cave.name %></h3>
      <p>Check-in date: <%= booking.checkin %></p>
      <p>Check-out date: <%= booking.checkout %></p>
      <!-- <p>Reservation Status: <%= booking.status%></p> -->
     </div>
<% end %>

    <div class="container">
      <h2 class="text-center">Your Upcoming Harvest</h2>
      <div class="cards">
        <!-- Code your cards  here -->

        <div class="card">
          <div class="card-body" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.1)), url('images/rio.jpg')">
            <span class="card-category">Rio</span>
            <div class="card-description">
              <h2>Casa de Cecile</h2>
              <p>Charming room in Rio</p>
            </div>
            <img class="card-user avatar avatar-large" src="images/cecile.png">
            <a class="card-filter" href="#" ></a>
          </div>
          <div class="card-footer">
            <p>Posted on 22/11/19</p>
            <div class="links">
              <a href="#"><i class="fa fa-camera-retro"></i></a></li>
              <a href="#"><i class="fa fa-coffee"></i></a></li>
              <a href="#"><i class="fa fa-poo"></i></a></li>
            </div>
          </div>
        </div>
        <div class="card">
          <div class="card-body" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.1)), url('images/berlin.jpg');">
            <span class="card-category">Berlin</span>
            <div class="card-description">
              <h2>Gab Haus</h2>
              <p>Charming room in Berlin</p>
            </div>
            <img class="avatar" src="images/jad.png">
            <a class="card-filter" href="#" ></a>
          </div>
          <div class="card-footer">
            <p>Posted on 22/11/19</p>
            <div class="links">
              <a href="#"><i class="fa fa-camera-retro"></i></a></li>
              <a href="#"><i class="fa fa-coffee"></i></a></li>
              <a href="#"><i class="fa fa-poo"></i></a></li>
            </div>
          </div>
        </div>
      </div>
    </div>


    <div class="wrapper-grey">

      <h2>Cards <strong>inside</strong> .col</h2>
      <div class="container">
        <div class="row">
          <div class="col-sm-12 col-md-6">
              <% @sent_bookings.each do |booking| %>
      <div class="card">
        <div class="card-body" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.1)), url(<%= booking.cave.photo %>);">
          <span class="card-category"><h3><%= booking.cave.name %></h3></span>
          <div class="card-description">
            <h2>Begins: <%= booking.checkout.strftime("%A, %B %d, %Y") %></h2>
          </div>
          <img class="avatar" src="<%= booking.cave.user.photo %>">
        </div>
                <div class="card-footer links">
<%= link_to 'Explore Cave', cave_path(booking.cave.id), class: 'btn btn-sm btn-green' %>
      <%= link_to 'Send Smoke Signal', "#", class: 'btn btn-sm btn-orange' %>
    <!-- <span><%#link_to 'Edit', edit_path(booking), class: 'btn btn-treehouse' %></span> -->
      <%= link_to 'Destroy!', booking_path(booking), class: 'btn btn-sm btn-red',  method: :delete,
                             data: { confirm: "This booking is carved in stone, but the Great Controller will ask #{booking.cave.user.first_name} #{booking.cave.user.last_name} to destroy it." } %>
        </div>
      </div>
    <% end %>
          </div>
          <div class="col-sm-12 col-md-6">
  <% @sent_bookings.each do |booking| %>
      <div class="card">
        <div class="card-body" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.1)), url(<%= booking.cave.photo %>);">
          <span class="card-category"><h3><%= booking.cave.name %></h3></span>
          <div class="card-description">
            <h2>Begins: <%= booking.checkout.strftime("%A, %B %d, %Y") %></h2>
          </div>
          <img class="avatar" src="<%= booking.cave.user.photo %>">
        </div>
        <div class="card-footer links">
<%= link_to 'Explore Cave', cave_path(booking.cave.id), class: 'btn btn-green btn-sm' %>
      <%= link_to 'Send Smoke Signal', "#", class: 'btn btn-orange btn-sm' %>
    <!-- <span><%#link_to 'Edit', edit_path(booking), class: 'btn btn-treehouse' %></span> -->
      <%= link_to 'Destroy!', booking_path(booking), class: 'btn btn-red btn-sm',  method: :delete,
                             data: { confirm: "This booking is carved in stone, but the Great Controller will ask #{booking.cave.user.first_name} #{booking.cave.user.last_name} to destroy it." } %>
        </div>
      </div>
    <% end %>
      </div>
    </div>

