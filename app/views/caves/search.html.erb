<div class="container text-center container-margin-top">

  <h1 id="index" class="index-h1">Your trophies!</h1>

    <div class="row">
        <% @caves.each do |cave| %>
          <div id="col" class="col-sm-3" >
            <div class="card">
              <%= cl_image_tag(cave.photo, width: 200, height: 175, crop: :fill, class: 'card-img-border') %>
              <div class="cave-card-body card-body">
                <h3 class="card-title"> <%= cave.name %></h3>
                <%= link_to 'Visit', cave_path(cave), class: "btn btn-orange btn-sm", id: "bottom-btn" %>
              </div>
            </div>
          </div>
        <% end %>
    </div>

</div>
